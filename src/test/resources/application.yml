spring:
# 스프링은 기본적인 설정이 없으면 자체적으로 내부 메모리 db를 사용한다.
#  datasource:
#    url: jdbc:h2:mem:test #내부 메모리 db를 사용해서 테스트할 수 있게 해준다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug

